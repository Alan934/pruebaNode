version: 1
applications:
  backend:
    phases:
      preBuild:
        commands:
          - npm ci
      build:
        commands:
          - npm run build # Si tienes un proceso de construcción en tu backend
    artifacts:
      baseDirectory: . # No se generan artefactos de frontend
      files:
        - '**/*'
    cache:
      paths:
        - node_modules/**/*

version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm install --prefer-offline
    build:
      commands:
        - cd frontend
        - npm run build
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*


